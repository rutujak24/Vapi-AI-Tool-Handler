# Vapi AI Tool Handler
A simple Python backend to handle function calls from a Vapi.ai voice agent.

## Setup
1. `pip install fastapi uvicorn`
2. Run with `python main.py`
3. Expose via `ngrok http 8000`
4. Add the URL to Vapi Dashboard under **Tools** -> **Custom Function**.
